The script needs to be interpreted in a venv with the discord package and the coinbase-advanced-trade package installed.

It requires the bot token, a Coinbase API public key as well es it's corresponding private key (only ECDSA algorythm keys work)
